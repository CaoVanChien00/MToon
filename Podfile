# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

pod 'SwiftSoup'

target 'MToon' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MToon

  target 'MToonTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
